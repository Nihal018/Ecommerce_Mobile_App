import { View, Text } from "react-native";

export default function PastPurchases({ navigation }) {
  return (
    <View className="bg-white h-full w-full">
      <Text> PastPurchases </Text>
    </View>
  );
}
